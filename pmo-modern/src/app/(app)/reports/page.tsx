import { ComingSoon } from "@/components/ComingSoon";

export const metadata = { title: "Relatórios — PMO CollabZ" };

export default function ReportsPage() {
  return (
    <ComingSoon
      title="Relatórios"
      session={9}
      description="Relatórios Detalhado / Consolidado / Criticidade / Comentários / KPIs com export XLSX e PDF."
    />
  );
}
